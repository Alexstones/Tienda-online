<section class="simple-page">
  <header>
    <p class="eyebrow">Ágappe · Spaces</p>
    <h1>Home decor</h1>
    <p class="subtitle">
      Objects that bring warmth, light and soul into every corner of your home.
    </p>
  </header>
</section>

<style>
  .simple-page {
    max-width: 1120px;
    margin: 0 auto;
  }

  header h1 {
    font-family: var(--font-serif);
    font-size: clamp(2.1rem, 3vw, 2.6rem);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin-bottom: 0.8rem;
    color: var(--petrol);
  }

  .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.18em;
    font-size: 0.75rem;
    margin-bottom: 0.6rem;
    color: var(--text-muted);
  }

  .subtitle {
    font-size: 1.02rem;
    max-width: 34rem;
    line-height: 1.7;
    color: var(--text-main);
  }
</style>
